# Baseline profile — hand-authored (no macrobenchmark harness). Covers the
# launcher's own code plus the Compose / TV-Material paths exercised on cold
# start and while scrolling, so ART compiles them ahead of time instead of
# interpreting them on first use. Flags: H=hot, S=startup, P=post-startup.

# --- The launcher itself: every class is on the startup / interaction path. ---
HSPLcom/gothwad/tvlauncher/**->**(**)**

# --- Compose runtime: composition, snapshots, recomposition. ---
HSPLandroidx/compose/runtime/**->**(**)**

# --- Compose UI: layout, draw, graphics, focus, input, first frame. ---
HSPLandroidx/compose/ui/**->**(**)**

# --- Compose foundation: lazy lists, gestures, scrolling, background/clip. ---
HSPLandroidx/compose/foundation/**->**(**)**

# --- Compose animation: the launch entrance + focus transitions. ---
HSPLandroidx/compose/animation/**->**(**)**

# --- TV Material 3 components used across every screen. ---
HSPLandroidx/tv/material3/**->**(**)**

# --- graphics-shapes: the squircle corner path built on every card. ---
HSPLandroidx/graphics/shapes/**->**(**)**

# --- Lifecycle + DataStore: read on startup. ---
HSPLandroidx/lifecycle/**->**(**)**
HSPLandroidx/datastore/**->**(**)**

# --- Coroutines + serialization: the whole cold path is coroutine-driven and
# gated on a kotlinx.serialization decode of the config blob; AOT them so the
# first launches after install/update don't run them interpreted. ---
HSPLkotlinx/coroutines/**->**(**)**
HSPLkotlinx/serialization/**->**(**)**
